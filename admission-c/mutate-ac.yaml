apiVersion: v1
kind: LimitRange
metadata:
  name: mutate-resources
  namespace: prod
spec:
  limits:
    - type: Container
      default:
        cpu: "100m"
        memory: "256Mi"
      defaultRequest:
        cpu: "50m"
        memory: "156Mi"
